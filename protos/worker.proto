syntax = "proto3";

service Worker {
    rpc MapOperation (MapInfo) returns (Result);
    rpc ReduceOperation (ReduceInfo) returns (Result);
}

message Result {
    string uuid = 1;
    bool result = 2;
}

message MapInfo {
  string filename = 1;
  string block = 2;
}

message ReduceInfo {
    string ip = 1;
    string filename = 2;
}
